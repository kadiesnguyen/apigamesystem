# =============================================================================
# Game System Environment Configuration Template
# =============================================================================
# Copy this file to .env and update the values for your environment
# DO NOT put real secrets or passwords in this file - it's tracked in Git!

# -----------------------------------------------------------------------------
# Project Configuration
# -----------------------------------------------------------------------------
COMPOSE_PROJECT_NAME=game_system
NODE_ENV=development

# -----------------------------------------------------------------------------
# Service Ports
# -----------------------------------------------------------------------------
API_PORT=3300
SERVER_PORT=3000
CMS_PORT=5173
POSTGRES_PORT=5432
REDIS_PORT=6379
MONGO_PORT=27017

# -----------------------------------------------------------------------------
# PostgreSQL Database Configuration
# -----------------------------------------------------------------------------
POSTGRES_DB=game
POSTGRES_USER=gameserver
POSTGRES_PASSWORD=your_secure_postgres_password_here
PG_HOST=postgres
PG_PORT=5432
PG_DATABASE=game

# -----------------------------------------------------------------------------
# Redis Configuration
# -----------------------------------------------------------------------------
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=your_secure_redis_password_here
REDIS_URL=redis://redis:6379

# -----------------------------------------------------------------------------
# MongoDB Configuration
# -----------------------------------------------------------------------------
MONGO_HOST=mongodb
MONGO_PORT=27017
MONGO_DB=logs
MONGO_DB_NAME=logs
MONGO_URL=mongodb://mongodb:27017
MONGO_URI=mongodb://mongodb:27017

# -----------------------------------------------------------------------------
# JWT & Authentication Configuration
# -----------------------------------------------------------------------------
JWT_SECRET=your_jwt_secret_here_make_it_long_and_random
JWT_ACCESS_SECRET=your_access_secret_here_make_it_long_and_random
JWT_REFRESH_SECRET=your_refresh_secret_here_make_it_long_and_random

# Token Expiration Settings
ACCESS_EXPIRES=60m                    # 1 hour
REFRESH_EXPIRES_DAYS=7               # 7 days (normal)
REFRESH_EXPIRES_DAYS_REM=30          # 30 days (remember me)

# -----------------------------------------------------------------------------
# API & Frontend URLs
# -----------------------------------------------------------------------------
# External URLs (for host access)
API_URL=http://localhost:3300
CMS_URL=http://localhost:5173
SERVER_URL=http://localhost:3000

# Internal URLs (for container-to-container communication)
VITE_API_URL=http://api:3300

# -----------------------------------------------------------------------------
# Cookie & Session Configuration  
# -----------------------------------------------------------------------------
COOKIE_DOMAIN=localhost

# -----------------------------------------------------------------------------
# Health Check Configuration
# -----------------------------------------------------------------------------
HEALTHCHECK_INTERVAL=10s
HEALTHCHECK_TIMEOUT=5s
HEALTHCHECK_RETRIES=5

# =============================================================================
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.template .env
# 2. Update all password fields with secure random values
# 3. Update JWT secrets with long random strings
# 4. Adjust ports if needed for your local environment
# 5. Never commit the real .env file to Git!
#
# Development Notes:
# - All services use these centralized environment variables
# - Internal container URLs use service names (api, postgres, redis, mongodb)
# - External URLs use localhost for host machine access
# =============================================================================